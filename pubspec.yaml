name: "SaveIt"
description: "A new Flutter project with authentication using Laravel and MySQL."

# Evita que la app se publique accidentalmente en pub.dev
publish_to: 'none'

# Configuración de versión de la app
version: 1.0.0+1

# Versión del SDK de Dart requerida
environment:
  sdk: '>=3.4.0 <4.0.0'

# Dependencias de la aplicación
dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter

  fl_chart: ^0.63.0
  
  http: ^1.4.0
  dio: ^5.4.0
  cherry_toast: ^1.1.0

  shared_preferences: ^2.2.2

  provider: ^6.0.5

  flutter_secure_storage: ^9.2.4

  cupertino_icons: ^1.0.6

# Dependencias solo necesarias para desarrollo y pruebas
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_login.png
    - lib/config/dev_config.json
    - assets/images/icon.png

flutter_icons:
  android: true
  ios: true
  image_path: assets/images/icon.png
